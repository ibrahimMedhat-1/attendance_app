import 'package:flutter/material.dart';

class AssignmentPageStudent extends StatelessWidget {
  const AssignmentPageStudent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
