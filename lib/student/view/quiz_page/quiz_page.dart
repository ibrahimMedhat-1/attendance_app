import 'package:flutter/material.dart';

class QuizPageStudent extends StatelessWidget {
  const QuizPageStudent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
